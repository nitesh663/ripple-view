# Dependencies (re-installed inside image)
node_modules
**/node_modules

# Build outputs (re-built inside image)
**/dist
**/.vite

# Version control
.git
.gitignore

# CI / local tooling
.husky
.vscode
.changeset
docker-compose.jenkins.yml

# Test fixtures and coverage
**/coverage
**/*.test.ts

# Secrets and environment
.env
.env.*
.npmrc
